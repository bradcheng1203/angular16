export interface Customer {
    id: string;
    name: string;
    email: string;
    phone: string;
    active: string
}

export interface Country{
    code:string,
    name:string
}

export interface Food {
  value: string;
  viewValue: string;  
}